# Contributing

## Setup
```bash
python -m venv venv && source venv/bin/activate
pip install -r requirements.txt
python -m pytest tests/
```

## Standards
- PEP 8 for Python, ESLint for JavaScript
- Type hints required, tests required for all features
